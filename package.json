{
  "name": "fnaf",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir dist && xcopy * dist /E /I /Y",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.0.0",
    "aws-serverless-express": "^3.4.0"
  }
}